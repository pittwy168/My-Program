Lintcode140_ 快速幂
计算a^n % b，其中a，b和n都是32位的整数。
class Solution {
public:
    /*
     * @param a, b, n: 32bit integers
     * @return: An integer
     */
    int fastPower(int a, int b, int n) {
        // write your code here
        if(b==1)return 0;
        long long a1=a%b; 
        long long pdt=1;
        while(n>=1){
            if(n%2==1)pdt=(pdt*a1)%b;
            a1=(a1*a1)%b;
            n/=2;
        }return pdt;
    }
};

